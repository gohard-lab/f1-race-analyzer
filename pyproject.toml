[project]
name = "f1-race-analyzer"
version = "0.1.0"
description = "F1 Race Analyzer Web App"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "streamlit",
    "supabase",
    "pandas",
    "requests"
]